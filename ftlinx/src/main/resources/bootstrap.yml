server:
  port: 8771

spring:
  application:
    name: ftlinx-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      profile: ftlinx-service

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
